<!DOCTYPE html><html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Plateforme Cours Pro</title>  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #0f172a, #1e293b);
      color: #fff;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      background: #111827;
      padding: 30px;
      border-radius: 12px;
      width: 350px;
      box-shadow: 0 0 20px rgba(0,0,0,0.5);
      text-align: center;
    }

    h2 {
      margin-bottom: 20px;
    }

    input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: none;
      border-radius: 8px;
      outline: none;
    }

    button {
      width: 100%;
      padding: 12px;
      margin-top: 10px;
      border: none;
      border-radius: 8px;
      background: #3b82f6;
      color: white;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    button:hover {
      background: #2563eb;
    }

    .section {
      margin-top: 20px;
      padding-top: 15px;
      border-top: 1px solid #374151;
    }

    .course-btn {
      background: #10b981;
    }

    .course-btn:hover {
      background: #059669;
    }

    .logout {
      background: #ef4444;
    }

    .logout:hover {
      background: #dc2626;
    }
  </style></head>
<body><div class="container">  <h2>Plateforme Cours</h2>  <input type="email" id="email" placeholder="Email">
  <input type="password" id="password" placeholder="Mot de passe"><button onclick="signup()">S'inscrire</button> <button onclick="login()">Se connecter</button> <button class="logout" onclick="logout()">Se déconnecter</button>

  <div class="section">
    <h3>📚 Cours disponibles</h3>
    <button class="course-btn" onclick="checkAccess('cours1.pdf')">Accéder au cours 1</button>
  </div></div><!-- Firebase SDK --><script src="https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js"></script><script src="https://www.gstatic.com/firebasejs/10.12.0/firebase-auth.js"></script><script src="https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js"></script><script>
const firebaseConfig = {
  apiKey: "AIzaSyAXZURCpn20cJxod8WbNUw5JaWLKoGaaD4",
  authDomain: "global-467b3.firebaseapp.com",
  projectId: "global-467b3",
  storageBucket: "global-467b3.appspot.com",
  messagingSenderId: "287601791180",
  appId: "1:287601791180:web:4ffc6696572553cac73269",
  measurementId: "G-7DJHC1TNJG"
};

firebase.initializeApp(firebaseConfig);
const auth = firebase.auth();
const db = firebase.firestore();

function signup() {
  const email = document.getElementById("email").value;
  const password = document.getElementById("password").value;

  auth.createUserWithEmailAndPassword(email, password)
    .then(userCredential => {
      const user = userCredential.user;

      db.collection("users").doc(user.uid).set({
        email: email,
        createdAt: new Date()
      });

      alert("Compte créé ✅");
    })
    .catch(error => alert(error.message));
}

function login() {
  const email = document.getElementById("email").value;
  const password = document.getElementById("password").value;

  auth.signInWithEmailAndPassword(email, password)
    .then(() => alert("Connecté ✅"))
    .catch(error => alert(error.message));
}

function logout() {
  auth.signOut().then(() => alert("Déconnecté ✅"));
}

auth.onAuthStateChanged(user => {
  if (user) {
    console.log("Connecté :", user.email);
  }
});

function savePurchase(transactionId, file) {
  const user = auth.currentUser;

  if (user) {
    db.collection("purchases").add({
      userId: user.uid,
      email: user.email,
      transactionId: transactionId,
      file: file,
      createdAt: new Date()
    });
  }
}

function checkAccess(file) {
  const user = auth.currentUser;

  if (!user) {
    alert("Connecte-toi d'abord ❌");
    return;
  }

  db.collection("purchases")
    .where("userId", "==", user.uid)
    .where("file", "==", file)
    .get()
    .then(snapshot => {
      if (!snapshot.empty) {
        window.location.href = file;
      } else {
        alert("Accès refusé ❌");
      }
    });
}

</script></body>
</html>
